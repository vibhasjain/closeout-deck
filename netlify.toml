[build]
  command = "cd shift-xray && npm install && npm run build && cd ../tasks && npm install && npm run build && cp -r dist/* ."
  publish = "."

[build.environment]
  NODE_VERSION = "20"

[[edge_functions]]
  function = "linear"
  path = "/api/linear"
