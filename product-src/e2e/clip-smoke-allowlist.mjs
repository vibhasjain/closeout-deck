// Only demonstrated false positives belong here. Product defects must keep failing.
// Each entry needs { surface, viewport, kind, element, reason, justification }.
// Match the stable element description and exact finding; never a generated marker.
export const allowlist = []
